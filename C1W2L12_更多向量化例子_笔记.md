# More Vectorization Examples | 更多向量化例子

> Andrew Ng《深度学习专项课程》Course 1, Week 2, Lesson 12 (C1W2L12)
> 视频:https://www.youtube.com/watch?v=pYWASRauTzs

## 核心知识点 | Key Points

### 1. 指导原则(重申)| The Guideline

- 神经网络编程守则:**只要有可能,就避免显式 for 循环**。
- 本讲给出更多"循环 → numpy 一行"的改写范例。
- The neural network programming guideline: whenever possible, avoid explicit for loops. This lecture shows more examples of the rewrite.

### 2. 例子一:矩阵乘向量 u = Av | Matrix × Vector

- 定义:uᵢ = Σⱼ Aᵢⱼ·vⱼ,非向量化要**两层循环**(遍历 i 和 j):

```python
# 非向量化:两层循环
u = np.zeros((n, 1))
for i in range(n):
    for j in range(m):
        u[i] += A[i][j] * v[j]

# 向量化:一行
u = np.dot(A, v)
```

- Matrix-vector multiplication needs a double loop if written by hand; `np.dot(A, v)` does it in one line.

### 3. 例子二:对每个元素做同一运算 | Elementwise Operations

- 需求:对向量 v 的**每个元素**求指数 e^(vᵢ):

```python
# 非向量化
u = np.zeros((n, 1))
for i in range(n):
    u[i] = math.exp(v[i])

# 向量化
u = np.exp(v)
```

- numpy 有一整族**逐元素函数 (elementwise functions)**:
  - `np.exp(v)` 指数、`np.log(v)` 对数、`np.abs(v)` 绝对值
  - `np.maximum(v, 0)` 每个元素和 0 取最大(就是 ReLU!)
  - `v**2` 每个元素平方、`1/v` 每个元素取倒数
- numpy has a whole family of elementwise functions: np.exp, np.log, np.abs, np.maximum(v, 0) (that's ReLU!), v**2, 1/v.

### 4. 用到逻辑回归上:先消掉内层循环 | Applying It: Kill the Inner Loop ⭐

- 回顾 L10 的算法有两层循环。本讲先消**内层**(遍历 n 个特征那层):
  - 原来:dw1 = 0; dw2 = 0; … 每个特征一个变量、一行累加
  - 现在:把 dw 当作**一个向量**处理:

```python
dw = np.zeros((n_x, 1))          # 初始化为向量

for i in range(m):               # 外层循环暂时保留
    z(i) = np.dot(w.T, x(i)) + b
    a(i) = σ(z(i))
    dz(i) = a(i) − y(i)
    dw += x(i) * dz(i)           # ⭐ 一行替代 dw1 += …, dw2 += …, …
    db += dz(i)

dw /= m; db /= m
```

- `dw += x(i)·dz(i)`:向量 x⁽ⁱ⁾ 整体乘标量 dz⁽ⁱ⁾,一行完成全部特征的累加。
- **进度:两层循环 → 一层循环**。剩下的外层(遍历样本)下一讲消灭。
- Instead of separate dw1, dw2, … accumulators, treat dw as a vector: dw += x(i)·dz(i) replaces the whole inner loop over features. Two loops → one loop; the outer loop over examples dies next lecture.

## 名词解释 | Glossary

| 英文 | 中文 | 解释 |
|---|---|---|
| Matrix × Vector (u = Av) | 矩阵乘向量 | uᵢ = Σⱼ Aᵢⱼvⱼ;np.dot(A, v) 一行完成 |
| Elementwise Operation | 逐元素运算 | 对向量每个元素独立做同一运算 |
| np.exp / np.log / np.abs | 逐元素函数 | 指数 / 对数 / 绝对值,整个向量一次算完 |
| np.maximum(v, 0) | 逐元素取最大 | 每个元素与 0 比大小取大者,即 ReLU |
| v**2, 1/v | 逐元素平方 / 倒数 | numpy 对整个向量的简写运算 |
| dw (as a vector) | 梯度向量 | 把 dw1…dwₙ 合并成一个 n_x 维向量统一累加 |
| Inner / Outer Loop | 内层 / 外层循环 | 特征循环(本讲消灭)/ 样本循环(下讲消灭)|
