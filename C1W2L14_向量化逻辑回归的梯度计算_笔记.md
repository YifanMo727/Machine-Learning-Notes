# Vectorizing Logistic Regression's Gradient Computation | 向量化逻辑回归的梯度计算

> Andrew Ng《深度学习专项课程》Course 1, Week 2, Lesson 14 (C1W2L14)
> 视频:https://www.youtube.com/watch?v=2BkqApHKwn0
> ⭐⭐ 整个向量化篇章的收官:两层 for 循环 → 五行代码

## 核心知识点 | Key Points

### 1. 回顾:上次留下的两层循环 | Recap: The Two Loops

- L10 的算法有两层循环:外层遍历 m 个样本,内层遍历 n_x 个特征。
- L12–L13 已经消灭了内层循环(dw 合并成向量)和前向传播的样本循环(Z = wᵀX + b)。
- 唯一剩下的:反向传播(算 dz、累加 dw、db)还在**逐样本**循环。
- We've already killed the inner (feature) loop and vectorized the forward pass. What's left: the backward pass still loops over the m examples one at a time.

### 2. 向量化 dz:一行算完 m 个误差 | Vectorizing dz

- 单样本公式:dz⁽ⁱ⁾ = a⁽ⁱ⁾ − y⁽ⁱ⁾。
- 把所有样本的 y 排成一行 **Y = [y⁽¹⁾, y⁽²⁾, …, y⁽ᵐ⁾]**(1×m,和 A 形状一致)。
- 于是:**dZ = A − Y**(逐元素相减,一次得到全部 m 个 dz)
- Stack all labels into a 1×m row vector Y (matching A's shape). Then dZ = A − Y computes all m errors in one elementwise subtraction.

### 3. 向量化 db:一次求和 | Vectorizing db

- 原来的 db 是把 m 个 dz⁽ⁱ⁾ 累加再除以 m,现在:
  - **db = (1/m) · np.sum(dZ)**
- np.sum 把 dZ 的所有元素加起来,一行搞定。
- db = (1/m)·np.sum(dZ): add up all elements of dZ in one call.

### 4. 向量化 dw:矩阵乘法自动累加求和 | Vectorizing dw ⭐

- 原来的 dw 需要:对每个样本,把 x⁽ⁱ⁾ 乘 dz⁽ⁱ⁾ 再累加到 dw 上。
- 向量化写法:
  - **dw = (1/m) · X · dZᵀ**
- 展开看为什么成立:X 是 (n_x, m),dZᵀ 是 (m, 1),相乘得到 (n_x, 1) —— 矩阵乘法的定义正好是"对应列相乘再对 m 求和",与手写累加循环做的事完全一致。
- dw = (1/m)·X·dZᵀ: X (n_x × m) times dZᵀ (m × 1) gives (n_x × 1). Matrix multiplication inherently sums over the m examples — exactly what the loop used to do.

### 5. 最终成果:一次迭代,零循环 | The Payoff: One Iteration, Zero Loops ⭐⭐

```python
Z  = np.dot(w.T, X) + b      # 前向:1×m
A  = sigma(Z)                 # 前向:1×m
dZ = A - Y                    # 反向:1×m
dw = (1/m) * np.dot(X, dZ.T)  # 反向:(n_x, 1)
db = (1/m) * np.sum(dZ)       # 反向:标量

w  = w - alpha * dw           # 参数更新
b  = b - alpha * db
```

- 对比 L10 的两层 for 循环,**这七行代码完成了完全相同的一次梯度下降迭代**,且没有任何显式循环。
- 唯一还需要 for 循环的地方:**多次迭代**本身(迭代 1000 次,就还要一个外层 for)——因为每次迭代依赖上一次的结果,这个循环无法向量化。
- Compare to L10's nested loops: these seven lines do exactly the same work for one iteration, with zero explicit loops over examples or features. The only remaining loop is over iterations themselves — that can't be vectorized because each iteration depends on the previous one.

## 完整公式速查 | Full Cheat Sheet

| 步骤 | 公式 | 形状 |
|---|---|---|
| 前向 Z | Z = wᵀX + b | (1, m) |
| 前向 A | A = σ(Z) | (1, m) |
| 反向 dZ | dZ = A − Y | (1, m) |
| 反向 dw | dw = (1/m)·X·dZᵀ | (n_x, 1) |
| 反向 db | db = (1/m)·np.sum(dZ) | 标量 |

## 名词解释 | Glossary

| 英文 | 中文 | 解释 |
|---|---|---|
| dZ = A − Y | 向量化误差 | 所有样本的 dz 一次算完,逐元素相减 |
| np.sum(dZ) | numpy 求和函数 | 把数组所有元素加起来 |
| dw = (1/m)·X·dZᵀ | 向量化权重梯度 | 矩阵乘法自动完成"乘后对 m 求和" |
| One Iteration | 一次迭代 | 一次完整的前向+反向+参数更新 |
| Loop Over Iterations | 迭代循环 | 唯一无法消除的循环,因为逐次依赖 |
| Fully Vectorized Logistic Regression | 完全向量化的逻辑回归 | 一次迭代仅需 5 行核心公式,零样本/特征循环 |
