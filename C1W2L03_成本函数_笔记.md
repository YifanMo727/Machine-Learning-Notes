# Logistic Regression Cost Function | 逻辑回归的成本函数

> Andrew Ng《深度学习专项课程》Course 1, Week 2, Lesson 3 (C1W2L03)
> 视频:https://www.youtube.com/watch?v=SHEPb1JHw5o

## 核心知识点 | Key Points

### 1. 我们要衡量什么 | What We Need to Measure

- 上一讲:ŷ = σ(wᵀx + b)。训练目标:让每个样本的预测 **ŷ⁽ⁱ⁾ ≈ y⁽ⁱ⁾**。
- 需要一个函数来衡量"预测得好不好"——这就是**损失函数 (loss function)**。
- We want ŷ⁽ⁱ⁾ ≈ y⁽ⁱ⁾ on the training set. The loss function measures how good the prediction ŷ is compared to the true label y.

### 2. 为什么不用平方误差 | Why Not Squared Error

- 直觉上可以用 **L = ½(ŷ − y)²**,但在逻辑回归里**不好用**:
  - 它会让优化问题变成**非凸 (non-convex)**——存在**多个局部最优 (local optima)**,梯度下降可能找不到**全局最优 (global optimum)**。
- Squared error makes the optimization problem non-convex: gradient descent may get stuck in local optima instead of finding the global optimum.

### 3. 逻辑回归的损失函数 | The Loss Function

- **L(ŷ, y) = −[ y·log(ŷ) + (1−y)·log(1−ŷ) ]**
- 直觉理解(分两种情况):
  - **y = 1 时**:L = −log(ŷ) → 要让损失小,就要 **ŷ 尽量大**(接近 1)✓
  - **y = 0 时**:L = −log(1−ŷ) → 要让损失小,就要 **ŷ 尽量小**(接近 0)✓
- 这个函数起到与平方误差类似的作用,但优化问题是**凸的 (convex)**,只有一个全局最优。
- L(ŷ, y) = −[y log ŷ + (1−y) log(1−ŷ)]. If y=1, minimizing loss pushes ŷ toward 1; if y=0, it pushes ŷ toward 0. And it gives a convex optimization problem.

### 4. 损失函数 vs 成本函数 | Loss vs Cost ⭐

- **损失函数 (loss function) L**:衡量**单个**训练样本的表现。
- **成本函数 (cost function) J**:整个训练集上损失的**平均值**,衡量参数 w, b 的整体表现:
  - **J(w, b) = (1/m) Σᵢ L(ŷ⁽ⁱ⁾, y⁽ⁱ⁾) = −(1/m) Σᵢ [ y⁽ⁱ⁾log ŷ⁽ⁱ⁾ + (1−y⁽ⁱ⁾)log(1−ŷ⁽ⁱ⁾) ]**
- The **loss** function measures performance on a single example; the **cost** function J(w, b) is the average loss over the entire training set.

### 5. 训练目标 | The Training Objective

- 训练 = 找到让 **J(w, b) 最小**的参数 w 和 b。
- 怎么找?**梯度下降 (gradient descent)**——下一讲内容。
- 视角:逻辑回归可以看作一个**极小的神经网络**。
- Training means finding w, b that minimize J(w, b) — using gradient descent (next lecture). Logistic regression can be viewed as a very small neural network.

## 名词解释 | Glossary

| 英文 | 中文 | 解释 |
|---|---|---|
| Loss / Error Function L(ŷ, y) | 损失函数 | 衡量单个样本预测好坏的函数 |
| Cost Function J(w, b) | 成本函数 | 整个训练集上损失的平均;训练要最小化的目标 |
| Squared Error | 平方误差 | ½(ŷ−y)²;逻辑回归中会导致非凸问题,不采用 |
| Convex | 凸(函数/问题) | 只有一个全局最优,梯度下降一定能找到 |
| Non-convex | 非凸 | 有多个局部最优,梯度下降可能被困住 |
| Local Optimum | 局部最优 | 局部范围内最好、但不是全局最好的解 |
| Global Optimum | 全局最优 | 整个参数空间中最好的解 |
| log(ŷ) | 对数 | 损失函数的核心;−log(ŷ) 在 ŷ→1 时趋于 0 |
| Cross-entropy Loss | 交叉熵损失 | 本讲损失函数的通用名称(课程后面会用到) |
| Optimization Problem | 优化问题 | 寻找使目标函数最小的参数的过程 |
