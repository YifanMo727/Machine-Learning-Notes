# What is a Neural Network? | 什么是神经网络?

> Andrew Ng《深度学习专项课程》Course 1, Week 1, Lesson 2 (C1W1L02)
> 视频:https://www.youtube.com/watch?v=n1l-9lIMW7E

## 核心知识点 | Key Points

### 1. 从房价预测开始 | Housing Price Prediction Example

- 用最简单的例子引入:输入房屋面积 (size),预测房价 (price)。这个"面积 → 价格"的函数,就可以看作一个最小的神经网络——**单个神经元 (a single neuron)**。
- The simplest possible neural network: one input (house size), one output (price). This single function IS a neuron.

### 2. ReLU 函数 | Rectified Linear Unit

- 拟合房价的函数先是 0(价格不会为负),然后是一条向上的直线。这种"先取零、再线性"的函数叫 **ReLU(修正线性单元)**,读作 "ray-lu",是深度学习中最常用的激活函数之一。
- **Rectify(修正)的意思就是取 max(0, x)** —— 把负数部分"修正"为 0。
- The fitted function starts at zero then goes up as a straight line — this "max(0, x)" shape is called ReLU, one of the most common activation functions.

### 3. 堆叠神经元构建更大的网络 | Stacking Neurons

- 更大的网络就是把很多神经元像乐高积木一样"堆叠"起来。课程例子:
  - 面积 + 卧室数 → 家庭规模 (family size)
  - 邮编 → 步行便利性 (walkability)
  - 邮编 + 富裕程度 → 学校质量 (school quality)
  - 这三个中间量再共同决定房价
- Larger networks are built by stacking many neurons, like Lego bricks.

### 4. 隐藏单元与全连接 | Hidden Units & Dense Connections

- 实际上你不需要手动指定"家庭规模、学校质量"这些中间特征——网络会自己学出来。**隐藏层 (hidden layer)** 的每个单元都接收**所有**输入特征,这叫**全连接/密集连接 (densely connected)**。
- You don't hand-design the middle features; the network figures them out. Every hidden unit connects to every input.

### 5. 核心思想 | The Big Idea

- 只要给足够多的 (x, y) 训练数据,神经网络非常擅长学出从 x 到 y 的精确映射函数。这也是它在**监督学习 (supervised learning)** 中如此强大的原因。
- Given enough training data (x, y pairs), neural networks are remarkably good at figuring out functions that accurately map x to y.

## 名词解释 | Glossary

| 英文 | 中文 | 解释 |
|---|---|---|
| Neural Network | 神经网络 | 由多个神经元连接构成的、可学习输入到输出映射的模型 |
| Neuron | 神经元 | 网络的基本单元,接收输入并输出一个计算结果 |
| ReLU (Rectified Linear Unit) | 修正线性单元 | 激活函数 max(0, x):负数输出 0,正数原样输出;rectify = 修正为非负 |
| Activation Function | 激活函数 | 决定神经元输出形式的非线性函数 |
| Input Layer (x) | 输入层 | 接收原始特征(如面积、邮编)的一层 |
| Hidden Layer / Hidden Unit | 隐藏层 / 隐藏单元 | 输入和输出之间的层,其取值由网络自己学习,不在数据中直接给出 |
| Output Layer (y) | 输出层 | 给出最终预测(如房价) |
| Densely Connected | 全连接 / 密集连接 | 每个隐藏单元都与上一层所有输入相连 |
| Feature | 特征 | 描述样本的输入变量(size、#bedrooms、zip code 等) |
| Training Data | 训练数据 | 用于学习的 (x, y) 样本对 |
| Supervised Learning | 监督学习 | 用带标签数据学习 x → y 映射的学习方式(下一讲主题) |
