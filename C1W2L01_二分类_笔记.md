# Binary Classification | 二分类

> Andrew Ng《深度学习专项课程》Course 1, Week 2, Lesson 1 (C1W2L01)
> 视频:https://www.youtube.com/watch?v=eqEc66RFY0I
> ⭐ 本讲的符号约定 (notation) 会贯穿整个课程,非常重要

## 核心知识点 | Key Points

### 1. 什么是二分类 | What is Binary Classification

- 输出 y 只有两种取值:**y ∈ {0, 1}**。例子:输入一张图片,判断"是猫 (1) 还是不是猫 (0)"。
- **逻辑回归 (logistic regression)** 是处理二分类的经典算法,是接下来几讲的主角。
- Binary classification: the output y is either 0 or 1 — e.g., "cat (1) vs non-cat (0)". Logistic regression is the classic algorithm for this.

### 2. 图片如何变成输入向量 x | Representing an Image as a Feature Vector

- 计算机中一张彩色图片 = **三个矩阵**,分别对应 **红 (Red)、绿 (Green)、蓝 (Blue)** 三个通道的像素亮度值。
- 若图片是 64×64 像素,就有 3 个 64×64 矩阵。
- 把所有像素值**展开 (unroll)** 成一个长列向量 x,作为网络输入:
  - **n_x = 64 × 64 × 3 = 12288**(输入特征维度)
- A color image is stored as three matrices (R, G, B pixel intensities). Unroll all pixel values into one long feature vector x with dimension n_x = 64 × 64 × 3 = 12288.

### 3. 单个训练样本的记号 | Notation for a Single Example

- 一个训练样本记为 **(x, y)**,其中 x ∈ R^(n_x),y ∈ {0, 1}。
- m 个训练样本:**{(x⁽¹⁾, y⁽¹⁾), (x⁽²⁾, y⁽²⁾), …, (x⁽ᵐ⁾, y⁽ᵐ⁾)}**
- 上标 (i) 表示**第 i 个样本**。
- 需要区分时:m_train = 训练集大小,m_test = 测试集大小。
- A single example is (x, y); superscript (i) refers to the i-th training example; m (or m_train) is the number of training examples.

### 4. 矩阵 X:把样本按"列"堆叠 | The Matrix X (Stack Examples as Columns)

- 把每个样本 x⁽ⁱ⁾ 作为**一列**,拼成矩阵 X:
  - **X 的形状 = (n_x, m)**:n_x 行,m 列
- ⚠️ 注意:有些其他课程把样本按"行"堆叠,但本课程用**列堆叠**约定,因为这样**实现神经网络时代码更简单**。
- Python 检查:`X.shape == (n_x, m)`
- Stack each x⁽ⁱ⁾ as a **column** of matrix X, so X has shape (n_x, m). This column convention makes neural network implementation much easier.

### 5. 标签矩阵 Y | The Label Matrix Y

- 同样把标签按列堆叠:**Y = [y⁽¹⁾, y⁽²⁾, …, y⁽ᵐ⁾]**
- **Y 的形状 = (1, m)**
- Python 检查:`Y.shape == (1, m)`
- Stack the labels as columns too: Y = [y⁽¹⁾ … y⁽ᵐ⁾], with shape (1, m).

## 名词解释 | Glossary

| 英文 | 中文 | 解释 |
|---|---|---|
| Binary Classification | 二分类 | 输出只有 0/1 两种取值的分类问题 |
| Logistic Regression | 逻辑回归 | 处理二分类的经典算法(下一讲主角) |
| Label | 标签 | 样本的正确答案 y(如 1=猫, 0=非猫) |
| Feature Vector (x) | 特征向量 | 把输入(如图片像素)展开成的长向量 |
| n_x | 输入特征维度 | 特征向量 x 的长度,如 64×64×3 = 12288 |
| Unroll / Reshape | 展开 / 重塑 | 把矩阵中所有值拉平成一个向量的操作 |
| RGB Channels | RGB 通道 | 彩色图片的红、绿、蓝三个像素矩阵 |
| Training Example (x⁽ⁱ⁾, y⁽ⁱ⁾) | 训练样本 | 上标 (i) 表示第 i 个样本 |
| m / m_train / m_test | 样本数 | 训练集 / 测试集的样本数量 |
| Matrix X | 输入矩阵 | 样本按列堆叠,形状 (n_x, m) |
| Matrix Y | 标签矩阵 | 标签按列堆叠,形状 (1, m) |
| X.shape | 矩阵形状 | Python/NumPy 中查看矩阵维度的命令 |
