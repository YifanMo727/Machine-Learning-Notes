# Computation Graph | 计算图

> Andrew Ng《深度学习专项课程》Course 1, Week 2, Lesson 7 (C1W2L07)
> 视频:https://www.youtube.com/watch?v=hCP1vGoCdYU

## 核心知识点 | Key Points

### 1. 为什么需要计算图 | Why a Computation Graph

- 神经网络的计算分两步:**前向传播 (forward pass)** 算出输出/成本,**反向传播 (backward pass)** 算出梯度。
- **计算图 (computation graph)** 把一个复杂计算拆成一步步的小运算,能清楚地解释这两个过程为什么这样组织。
- A neural network's computation is organized as a forward pass (compute the output/cost) and a backward pass (compute gradients). The computation graph explains why.

### 2. 例子:J = 3(a + bc) | The Running Example

- 把计算拆成三步:
  1. **u = b·c**
  2. **v = a + u**
  3. **J = 3v**
- 画成图:`b, c → [u = bc] → [v = a + u] → [J = 3v]`(a 直接进入第二步)
- 代入 a = 5, b = 3, c = 2:
  - u = 3×2 = **6**
  - v = 5 + 6 = **11**
  - J = 3×11 = **33**
- Break J(a,b,c) = 3(a + bc) into three steps: u = bc, v = a + u, J = 3v. With a=5, b=3, c=2: u=6, v=11, J=33.

### 3. 什么时候计算图有用 | When It's Useful

- 当有一个**特别关心的输出变量**需要优化时(比如逻辑回归的**成本函数 J**),计算图最有用。
- 神经网络里我们要**最小化 J**,J 就是图最右端的输出节点。
- The graph is most useful when there is one distinguished output variable you want to optimize — like the cost J in logistic regression.

### 4. 两个方向 | Two Directions ⭐

- **从左到右 (left-to-right)**:前向计算,得到 **J 的值**。
- **从右到左 (right-to-left)**:反向计算,得到**导数/梯度**(下一讲内容)。
- A left-to-right (forward) pass computes the value of J. A right-to-left (backward) pass is the natural way to compute derivatives — next lecture.

## 名词解释 | Glossary

| 英文 | 中文 | 解释 |
|---|---|---|
| Computation Graph | 计算图 | 把复杂计算拆成一步步小运算并连成的图 |
| Forward Pass / Forward Propagation | 前向传播 | 从左到右计算,得到输出(如成本 J) |
| Backward Pass / Backward Propagation | 反向传播 | 从右到左计算,得到导数/梯度 |
| Node | 节点 | 图中的一步运算(如 u = bc) |
| Intermediate Variable | 中间变量 | 拆步骤时引入的量(如 u、v) |
| Output Variable (J) | 输出变量 | 图最右端、我们想优化的量(如成本函数) |
| Left-to-right Computation | 从左到右计算 | 前向传播的方向 |
| Right-to-left Computation | 从右到左计算 | 反向传播(求导)的方向 |
