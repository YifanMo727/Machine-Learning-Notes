# Vectorization | 向量化

> Andrew Ng《深度学习专项课程》Course 1, Week 2, Lesson 11 (C1W2L11)
> 视频:https://www.youtube.com/watch?v=qsIrQi0fzbY
> 解决上一讲留下的问题:for 循环太慢

## 核心知识点 | Key Points

### 1. 什么是向量化 | What is Vectorization

- **向量化 (vectorization) = 去掉代码里的显式 for 循环**,用矩阵/向量运算替代。
- 深度学习中数据集巨大,代码运行速度直接决定你等结果是 1 分钟还是 1 天,向量化是**必备技能**。
- Vectorization is the art of getting rid of explicit for loops, replacing them with matrix/vector operations. With huge datasets, it decides whether you wait minutes or days.

### 2. 例子:计算 z = wᵀx + b | The Example

- w, x 都是 n_x 维向量。两种实现:

```python
# 非向量化(慢) Non-vectorized
z = 0
for i in range(n_x):
    z += w[i] * x[i]
z += b

# 向量化(快) Vectorized
z = np.dot(w, x) + b       # np.dot(w, x) 就是 wᵀx
```

- `np.dot(w, x)` 一行完成整个内积运算。
- Non-vectorized: loop over every component. Vectorized: `z = np.dot(w, x) + b` — one line for the whole inner product.

### 3. 速度实测:约 300 倍差距 | The Speed Demo ⭐

- 课程演示:两个 100 万维的随机向量做内积:
  - **np.dot 版本:约 1.5 毫秒**
  - **for 循环版本:约 480 毫秒**
  - 相差约 **300 倍**!
- 换算:向量化 1 分钟能跑完的任务,for 循环要 5 个小时。
- Demo with two 1,000,000-dimensional vectors: np.dot takes ~1.5 ms, the for loop ~480 ms — roughly 300× slower. What takes 1 minute vectorized would take ~5 hours with loops.

### 4. 为什么快:SIMD 并行 | Why It's Fast: SIMD

- CPU 和 GPU 都有 **SIMD 指令 (Single Instruction, Multiple Data,单指令多数据)**:一条指令同时对多个数据做同样的运算。
- numpy 的内置函数(np.dot 等)能利用这种**并行 (parallelism)**;自己写的 for 循环则完全用不上。
- GPU 尤其擅长 SIMD 运算,但 **CPU 也没有那么差**——向量化在 CPU 上同样有巨大收益。
- Both CPUs and GPUs have SIMD (Single Instruction, Multiple Data) instructions. Built-in numpy functions exploit this parallelism; your handwritten for loops don't. GPUs are especially good at SIMD, but CPUs benefit hugely too.

### 5. 经验法则 | Rule of Thumb ⭐

- **只要有可能,就避免显式 for 循环 (Whenever possible, avoid explicit for loops.)**
- 看到循环先想:能不能用 numpy 内置函数(np.dot、np.exp 等)替代?
- Whenever possible, avoid explicit for loops — always ask if a built-in numpy function can do the job.

## 名词解释 | Glossary

| 英文 | 中文 | 解释 |
|---|---|---|
| Vectorization | 向量化 | 用矩阵/向量运算替代显式 for 循环的技术 |
| np.dot(w, x) | 内积函数 | numpy 计算 wᵀx 的函数,底层高度优化 |
| SIMD | 单指令多数据 | 一条指令并行处理多个数据的硬件能力 |
| Parallelism | 并行 | 同时执行多个运算;向量化快的根本原因 |
| Built-in Function | 内置函数 | numpy 提供的优化函数(np.dot、np.exp 等)|
| Explicit For Loop | 显式 for 循环 | 手写的逐元素循环;应尽量避免 |
| CPU / GPU | 中央/图形处理器 | 都支持 SIMD;GPU 更擅长,CPU 也受益 |
