# Logistic Regression Gradient Descent | 逻辑回归的梯度下降

> Andrew Ng《深度学习专项课程》Course 1, Week 2, Lesson 9 (C1W2L09)
> 视频:https://www.youtube.com/watch?v=z_xiwjEdAC4
> ⭐ 把计算图和链式法则用到真正的模型上(单个样本)

## 核心知识点 | Key Points

### 1. 单样本的计算图 | The Computation Graph (One Example)

- 设有两个特征 x₁, x₂,前向计算三步:
  1. **z = w₁x₁ + w₂x₂ + b**
  2. **a = σ(z)**(a 就是预测值 ŷ)
  3. **L(a, y) = −[ y·log a + (1−y)·log(1−a) ]**
- 图:`x₁, w₁, x₂, w₂, b → [z] → [a] → [L]`
- 目标:计算 L 对 w₁, w₂, b 的导数,然后更新参数。
- Forward: z = w₁x₁ + w₂x₂ + b, then a = σ(z), then the loss L(a, y). We want the derivatives of L with respect to w₁, w₂, b.

### 2. 反向传播第一步:da | Backward Step 1

- **da = dL/da = −y/a + (1−y)/(1−a)**
- First backward step: da = dL/da = −y/a + (1−y)/(1−a).

### 3. 反向传播第二步:dz(最重要的结果)| Backward Step 2 ⭐⭐

- 由链式法则 dz = da · (da/dz),其中 σ 的导数 da/dz = a(1−a),代入化简得:
  - **dz = dL/dz = a − y**
- 这个结果极其简洁:**预测值减真实值**。后面所有实现都直接用它。
- By the chain rule, dz = da · a(1−a), which simplifies beautifully to dz = a − y (prediction minus label). This is the single most important formula of the lecture.

### 4. 最后一步:参数的导数 | Derivatives for the Parameters

- **dw₁ = x₁ · dz**
- **dw₂ = x₂ · dz**
- **db = dz**
- The parameter derivatives: dw₁ = x₁·dz, dw₂ = x₂·dz, db = dz.

### 5. 参数更新 | The Update

- 一步梯度下降:
  - w₁ := w₁ − α·dw₁
  - w₂ := w₂ − α·dw₂
  - b := b − α·db
- 这是**单个样本**上的一次更新;下一讲扩展到 m 个样本。
- Update each parameter: w := w − α·dw, b := b − α·db. This is for a single example; next lecture extends to m examples.

## 反向传播公式速查 | Backprop Cheat Sheet

| 量 | 公式 | 含义 |
|---|---|---|
| da | −y/a + (1−y)/(1−a) | L 对 a 的导数 |
| dz | **a − y** | L 对 z 的导数(最关键)|
| dw₁ | x₁ · dz | L 对 w₁ 的导数 |
| dw₂ | x₂ · dz | L 对 w₂ 的导数 |
| db | dz | L 对 b 的导数 |

## 名词解释 | Glossary

| 英文 | 中文 | 解释 |
|---|---|---|
| a (activation) | 激活值 | σ(z) 的输出,即预测值 ŷ |
| da, dz, dw, db | 反向传播变量 | 代码记号:损失 L 对各量的导数 |
| dz = a − y | 核心公式 | 预测值减真实值;逻辑回归反向传播的关键结果 |
| Derivative of Sigmoid | Sigmoid 的导数 | dσ/dz = σ(z)(1−σ(z)) = a(1−a) |
| One Step of Gradient Descent | 一步梯度下降 | 用当前梯度对参数做一次更新 |
| Single Training Example | 单个训练样本 | 本讲只考虑一个 (x, y);m 个样本见下一讲 |
