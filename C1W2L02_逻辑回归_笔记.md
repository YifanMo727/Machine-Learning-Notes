# Logistic Regression | 逻辑回归

> Andrew Ng《深度学习专项课程》Course 1, Week 2, Lesson 2 (C1W2L02)
> 视频:https://www.youtube.com/watch?v=hjrYrynGWGA

## 核心知识点 | Key Points

### 1. 逻辑回归要解决什么问题 | The Goal

- 给定输入 x(如一张图片的特征向量),输出预测值:
  - **ŷ = P(y = 1 | x)**:在给定 x 的条件下,y = 1(是猫)的**概率**。
- 因为 ŷ 是概率,必须满足 **0 ≤ ŷ ≤ 1**。
- Given x, we want ŷ = P(y = 1 | x) — the probability that y equals 1. Since it's a probability, ŷ must be between 0 and 1.

### 2. 参数 w 和 b | Parameters

- 逻辑回归的参数有两个:
  - **w ∈ R^(n_x)**:权重向量 (weights),维度与 x 相同
  - **b ∈ R**:偏置 (bias),一个实数
- The parameters of logistic regression: w (an n_x-dimensional weight vector) and b (a real number, the bias).

### 3. 为什么不能直接用线性函数 | Why Not Linear Regression

- 直接输出 **ŷ = wᵀx + b**(线性回归的做法)不行:
  - 结果可能 **大于 1**,也可能是 **负数**——不是合法的概率。
- Linear output wᵀx + b doesn't work: it can be bigger than 1 or negative, which makes no sense for a probability.

### 4. 解决方法:套上 Sigmoid | The Sigmoid Solution

- 把线性输出记为 **z = wᵀx + b**,再套上 sigmoid 函数:
  - **ŷ = σ(z) = 1 / (1 + e^(−z))**
- Sigmoid 的性质:
  - z 很大 → e^(−z) ≈ 0 → **σ(z) ≈ 1**
  - z 是很大的负数 → e^(−z) 很大 → **σ(z) ≈ 0**
  - z = 0 → **σ(0) = 0.5**
  - 输出永远在 (0, 1) 之间,平滑地从 0 过渡到 1 ✓
- Apply the sigmoid function to z = wᵀx + b: σ(z) = 1/(1+e^(−z)). If z is large, σ(z) ≈ 1; if z is a large negative number, σ(z) ≈ 0; σ(0) = 0.5. The output is always a valid probability.

### 5. 符号约定:w 和 b 分开 | Notation: Keep w and b Separate

- 有些课程把 b 合并进 w:定义 x₀ = 1,用一个参数向量 θ(θ₀ 就是 b)。
- **本课程不用这种约定**:w 和 b 分开处理,**实现神经网络时更简单**。
- Some courses merge b into the parameter vector (θ notation with x₀ = 1). This course keeps w and b as separate parameters — it makes neural network implementation easier.

### 6. 学习目标 | What Learning Means

- 训练逻辑回归 = **学习参数 w 和 b**,使 ŷ 成为对 P(y = 1 | x) 的良好估计。
- 怎么学?需要定义**损失函数 (loss function)**——下一讲的内容。
- Training means learning w and b so that ŷ becomes a good estimate of P(y = 1 | x). To do that we need to define a cost function — next lecture.

## 名词解释 | Glossary

| 英文 | 中文 | 解释 |
|---|---|---|
| Logistic Regression | 逻辑回归 | 输出 0~1 概率的二分类算法,神经网络的基础单元 |
| ŷ (y-hat) | 预测值 | 模型输出:P(y=1 | x) 的估计 |
| P(y = 1 \| x) | 条件概率 | 给定输入 x 时 y=1 的概率 |
| w (weights) | 权重向量 | 与 x 同维度的参数,决定每个特征的影响力 |
| b (bias) | 偏置 / 截距 | 一个实数参数,平移决策边界 |
| z | 线性输出 | z = wᵀx + b,套 sigmoid 之前的值 |
| wᵀx | w 转置乘 x | 向量内积:各特征加权求和 |
| Sigmoid Function σ(z) | Sigmoid 函数 | 1/(1+e^(−z));把任意实数压缩到 (0,1) |
| Linear Regression | 线性回归 | 直接输出 wᵀx + b;不适合二分类(输出可能越界) |
| Parameters | 参数 | 模型中需要通过训练学习的量(这里是 w 和 b) |
| θ (theta) Notation | θ 记号 | 其他课程把 b 并入参数向量的写法;本课程不采用 |
