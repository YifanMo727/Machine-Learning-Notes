# Gradient Descent | 梯度下降

> Andrew Ng《深度学习专项课程》Course 1, Week 2, Lesson 4 (C1W2L04)
> 视频:https://www.youtube.com/watch?v=uJryes5Vk1o

## 核心知识点 | Key Points

### 1. 要解决的问题 | The Setup

- 成本函数 **J(w, b)** 是**凸函数 (convex)**——像一个大碗,只有一个全局最优。
- 目标:找到让 J(w, b) 最小的参数 w, b。
- J(w, b) is a convex, bowl-shaped surface. We want to find the values of w and b that minimize it.

### 2. 初始化 | Initialization

- 先给 w, b 一个初始值。逻辑回归通常**初始化为 0**(随机初始化也行)。
- 因为 J 是凸的,**无论从哪里出发**,梯度下降都会走到同一个全局最优。
- Initialize w and b (usually to zero for logistic regression). Because J is convex, gradient descent reaches the same global optimum regardless of initialization.

### 3. 梯度下降的更新规则 | The Update Rule ⭐

- 重复执行:**沿最陡下坡方向走一小步 (steepest descent)**,直到收敛:

```
Repeat {
    w := w − α · dJ(w)/dw      # 代码里写作 dw
    b := b − α · dJ(w,b)/db    # 代码里写作 db
}
```

- **α(alpha)= 学习率 (learning rate)**:控制每一步走多大。
- **dJ/dw = 导数 (derivative)**:函数在当前点的**斜率 (slope)**。
- **:=** 表示"更新/赋值"。
- Repeatedly update w := w − α·dJ/dw and b := b − α·dJ/db. α is the learning rate (step size); the derivative is the slope of J at the current point.

### 4. 为什么它有效(直觉)| Why It Works

- 假设只有一个参数 w(一维碗形曲线):
  - w 在最低点**右侧** → 斜率为**正** → w := w − α·(正数) → **w 变小**,向最低点移动 ✓
  - w 在最低点**左侧** → 斜率为**负** → w := w − α·(负数) → **w 变大**,也向最低点移动 ✓
- 无论从哪边出发,更新都朝最低点走,最终**收敛 (converge)** 到全局最优。
- If w is to the right of the minimum, the slope is positive, so w decreases; if to the left, the slope is negative, so w increases. Either way, gradient descent moves toward the minimum.

### 5. 导数记号:d vs ∂ | Derivative Notation

- **d**(小写 d):函数只有**一个**变量时用,如 dJ(w)/dw。
- **∂**(偏导符号,partial derivative):函数有**两个及以上**变量时用,如 ∂J(w,b)/∂w。
- 意义完全相同——都是"J 在该方向上的斜率",只是微积分的书写惯例。
- **代码约定**:dJ/dw 就写作变量 `dw`,dJ/db 写作 `db`。
- Lowercase "d" is used when J has one variable; the "∂" symbol when J has two or more. They mean the same thing — the slope in that direction. In code we just use variable names dw and db.

## 名词解释 | Glossary

| 英文 | 中文 | 解释 |
|---|---|---|
| Gradient Descent | 梯度下降 | 反复沿最陡下坡方向更新参数、最小化 J 的算法 |
| Learning Rate (α) | 学习率 | 控制每步更新幅度的超参数 |
| Derivative (dJ/dw) | 导数 | 函数在当前点的斜率;决定更新方向 |
| Partial Derivative (∂) | 偏导数 | 多变量函数对其中一个变量的导数 |
| Slope | 斜率 | 导数的几何含义:高/宽的比值 |
| Convex Function | 凸函数 | 碗形;任意初始化都能收敛到全局最优 |
| Initialization | 初始化 | 给参数设定起始值;逻辑回归常用 0 |
| Converge | 收敛 | 迭代逐渐逼近最优点、不再明显变化 |
| Iteration | 迭代 | 梯度下降中的一次参数更新 |
| := (Update) | 赋值/更新 | 用新计算的值替换参数当前值 |
| dw, db | 代码变量名 | 程序中表示 dJ/dw、dJ/db 的约定写法 |
| Steepest Descent | 最陡下降方向 | 当前点下坡最快的方向,即负梯度方向 |
