# Gradient Descent on m Examples | m 个样本的梯度下降

> Andrew Ng《深度学习专项课程》Course 1, Week 2, Lesson 10 (C1W2L10)
> 视频:https://www.youtube.com/watch?v=KKfZLXcF-aE

## 核心知识点 | Key Points

### 1. 从单样本到 m 个样本 | From One Example to m

- 成本函数是所有样本损失的平均:**J(w, b) = (1/m) Σᵢ L(a⁽ⁱ⁾, y⁽ⁱ⁾)**
- 关键性质:**总梯度 = 各样本梯度的平均**:
  - dJ/dw₁ = (1/m) Σᵢ dw₁⁽ⁱ⁾
- 所以做法很直接:对每个样本算一遍上一讲的导数,再取平均。
- The cost is the average loss over m examples, so the overall gradient is just the average of the per-example gradients from last lecture.

### 2. 完整算法(一次迭代)| One Iteration of the Algorithm ⭐

```
J = 0; dw1 = 0; dw2 = 0; db = 0          # 初始化累加器

for i = 1 to m:                           # 遍历样本
    z(i) = w·x(i) + b
    a(i) = σ(z(i))
    J += −[ y(i)·log a(i) + (1−y(i))·log(1−a(i)) ]
    dz(i) = a(i) − y(i)
    dw1 += x1(i) · dz(i)                  # 累加
    dw2 += x2(i) · dz(i)
    db  += dz(i)

J /= m; dw1 /= m; dw2 /= m; db /= m      # 取平均

w1 := w1 − α·dw1                          # 一步梯度下降
w2 := w2 − α·dw2
b  := b − α·db
```

- 注意:这里 dw₁, dw₂, db 是**累加器 (accumulator)**,累积的是整个训练集的梯度。
- 以上只是**一次迭代**;要重复多次才能收敛。
- One pass over all m examples accumulates the gradients (dw1, dw2, db act as accumulators), then divide by m and take one gradient descent step. Repeat many iterations to converge.

### 3. 这个实现的弱点:两个 for 循环 | The Weakness: Two For Loops ⭐

- 上面的代码有**两层显式 for 循环**:
  1. 外层:遍历 **m 个样本**
  2. 内层:遍历 **n 个特征**(这里只有 dw₁、dw₂ 两个,特征多时要循环)
- **显式 for 循环让代码非常慢**,深度学习时代数据量巨大,这是致命的。
- The implementation has two explicit for loops: over the m examples and over the n features. Explicit for loops make code slow — deadly in the deep learning era of big datasets.

### 4. 解决方案预告:向量化 | The Fix: Vectorization

- **向量化 (vectorization)** 技术可以**完全去掉显式 for 循环**,大幅加速。
- 深度学习兴起前向量化是"锦上添花",现在是**必备技能**——下一讲开始学。
- Vectorization lets you get rid of explicit for loops entirely. It used to be nice-to-have; in the deep learning era it's essential. Next lectures cover it.

## 名词解释 | Glossary

| 英文 | 中文 | 解释 |
|---|---|---|
| Gradient on m Examples | m 样本梯度 | 各样本梯度的平均值 |
| Accumulator | 累加器 | 循环中不断累加的变量(dw₁, dw₂, db)|
| Superscript (i) | 上标 (i) | 第 i 个样本的量,如 z⁽ⁱ⁾, a⁽ⁱ⁾, dz⁽ⁱ⁾ |
| One Iteration | 一次迭代 | 遍历全部样本 + 一次参数更新;需重复多次 |
| Explicit For Loop | 显式 for 循环 | 逐个遍历的写法;深度学习中要尽量避免 |
| n (number of features) | 特征数 | 内层循环的规模;特征多时 dw 要逐个算 |
| Vectorization | 向量化 | 用矩阵/向量运算替代 for 循环的加速技术(下一讲)|
