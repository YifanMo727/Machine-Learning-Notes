# Why is Deep Learning Taking Off? | 为什么深度学习会兴起?

> Andrew Ng《深度学习专项课程》Course 1, Week 1, Lesson 4 (C1W1L04)
> 视频:https://www.youtube.com/watch?v=xflCLdJh0n0

## 核心知识点 | Key Points

### 1. 规模驱动深度学习的进步 | Scale Drives Deep Learning Progress

- 深度学习的思想几十年前就有,近年才爆发的核心原因:**规模 (scale)**——包括**数据规模**和**网络规模**。
- 关键图像:横轴是数据量 (amount of data),纵轴是性能 (performance):
  - **传统算法**(SVM、逻辑回归):数据变多后性能**趋于平缓 (plateau)**,不知如何利用海量数据。
  - **神经网络**:网络越大 (small → medium → large NN),性能随数据增长得越久、上限越高。
- 要达到高性能,需要:① 训练**足够大的网络**;② 投入**海量的数据**。
- The idea of deep learning has been around for decades. It's taking off now because of scale — of both data and neural networks. Traditional algorithms plateau as data grows; larger networks keep improving.

### 2. 数据从哪来 | The Rise of Data

- **社会数字化 (digitalization of society)** 带来了海量数据:手机、相机、传感器、物联网 (IoT) 不断产生带标签数据。
- 课程约定:**m 表示训练样本数量** (number of training examples)。
- 注意:在**小数据**场景下,各算法的相对表现并不确定,**手工设计特征 (hand-engineered features)** 的传统方法可能反而更好;只有在**大数据**场景下,大型神经网络才稳定占优。
- With small training sets, the ordering of algorithms is not well defined — skill at hand-engineering features can win. Only in the big-data regime do large neural nets dominate.

### 3. 三大驱动力 | Three Drivers: Data, Computation, Algorithms

- **数据 (Data)**:数字化带来的海量标签数据。
- **计算 (Computation)**:GPU 等硬件让训练大网络成为可能。
- **算法 (Algorithms)**:大量算法创新,很多是为了**让网络跑得更快**。
- The three forces: data, computation (GPUs, specialized hardware), and algorithmic innovation.

### 4. 算法创新的例子:Sigmoid → ReLU | Example: Sigmoid → ReLU

- **Sigmoid 函数**的问题:在两端区域**斜率(梯度)接近 0**,参数更新极慢,**梯度下降 (gradient descent)** 学习速度大幅变慢。
- **ReLU** 在正区间梯度恒为 1,不会"梯度消失",让梯度下降**快得多**。
- 这是"算法创新提升计算速度"的经典例子。
- Sigmoid's gradient is nearly zero at the extremes, making learning very slow. Switching to ReLU (gradient = 1 for positive inputs) makes gradient descent work much faster.

### 5. 快速迭代循环 | The Idea → Code → Experiment Cycle

- 训练更快的意义:开发是一个循环——**想法 (Idea) → 代码 (Code) → 实验 (Experiment)** → 再回到想法。
- 训练一次网络可能要 10 分钟、1 天甚至 1 个月。**训练越快,迭代越快,越容易尝试更多想法**,研究和产品效率大幅提高。
- Faster computation matters because building networks is iterative: idea → code → experiment. Faster training = faster iteration = more ideas tried.

### 6. 未来会继续变好 | Why Optimism is Warranted

- 三大驱动力都还在增强:数字化数据持续增长、专用硬件 (GPU/专用芯片) 越来越强、算法创新不断涌现。
- 因此有理由乐观:深度学习还会持续变得更好。
- All three forces — data, compute, algorithms — are still improving, so deep learning will keep getting better for years to come.

## 名词解释 | Glossary

| 英文 | 中文 | 解释 |
|---|---|---|
| Scale | 规模 | 数据规模 + 网络规模,驱动深度学习进步的核心 |
| Plateau | (性能)趋于平缓 | 传统算法在数据变多后性能不再提升的现象 |
| m (number of training examples) | 训练样本数 | 课程符号约定:m 表示训练集大小 |
| Labeled Data | 带标签数据 | 同时包含输入 x 和答案 y 的数据 |
| Hand-engineered Features | 手工设计特征 | 人工设计输入特征;小数据时代的重要技能 |
| Gradient Descent | 梯度下降 | 沿梯度方向逐步更新参数以降低误差的优化算法 |
| Gradient / Slope | 梯度 / 斜率 | 函数变化率;梯度接近 0 时学习变慢 |
| Sigmoid Function | Sigmoid 函数 | S 形激活函数,两端梯度接近 0,易导致学习缓慢 |
| ReLU | 修正线性单元 | max(0, x);正区间梯度为 1,使训练更快 |
| Digitalization | 数字化 | 社会活动转为数字记录,产生海量数据 |
| GPU | 图形处理器 | 使大规模神经网络训练可行的硬件 |
| Idea → Code → Experiment | 想法→代码→实验 | 深度学习的迭代开发循环,训练速度决定迭代效率 |
