# Derivatives With Computation Graphs | 用计算图求导

> Andrew Ng《深度学习专项课程》Course 1, Week 2, Lesson 8 (C1W2L08)
> 视频:https://www.youtube.com/watch?v=nJyUyKN-XBQ
> ⭐ 本讲引入链式法则和反向传播,是理解神经网络训练的关键

## 核心知识点 | Key Points

### 1. 从右到左求导 | Computing Derivatives Right-to-Left

- 沿用上一讲的图:u = bc → v = a + u → J = 3v(a=5, b=3, c=2 → J=33)。
- 求导时**从图的最右端往左**算,先算离 J 最近的:
  - **dJ/dv = 3**(v 从 11 → 11.001,J 从 33 → 33.003)
- The most efficient way to compute derivatives is a right-to-left pass. Start closest to the output: dJ/dv = 3.

### 2. 链式法则 | The Chain Rule ⭐⭐

- 求 dJ/da:a 变化 → v 变化 → J 变化。变化量相乘:
  - **dJ/da = (dJ/dv) × (dv/da) = 3 × 1 = 3**
- 这就是微积分的**链式法则 (chain rule)**:a 推动 v,v 再推动 J,总效果是两段"放大倍率"的乘积。
- 同理:
  - **dJ/du = (dJ/dv) × (dv/du) = 3 × 1 = 3**
  - **dJ/db = (dJ/du) × (du/db) = 3 × c = 3 × 2 = 6**
  - **dJ/dc = (dJ/du) × (du/dc) = 3 × b = 3 × 3 = 9**
- Chain rule: if a affects v and v affects J, then dJ/da = (dJ/dv)(dv/da). Similarly dJ/db = 3·c = 6 and dJ/dc = 3·b = 9.

### 3. 为什么从右到左最高效 | Why Right-to-Left is Efficient

- 计算 dJ/dv 之后,它可以**复用**于计算 dJ/da、dJ/du;dJ/du 又复用于 dJ/db、dJ/dc。
- 一次反向遍历即可得到 J 对**所有**变量的导数,不用重复计算。
- After computing dJ/dv once, it is reused for dJ/da and dJ/du; dJ/du is then reused for dJ/db and dJ/dc. One backward pass gives all derivatives efficiently.

### 4. 代码记号约定 | Notation in Code ⭐

- 反向传播中我们求的都是"**最终输出 J 对某变量的导数**" dJ/d(var)。
- 代码里统一简写为 **`dvar`**:
  - `dv` = dJ/dv = 3,`da` = dJ/da = 3,`du` = dJ/du = 3,`db` = dJ/db = 6,`dc` = dJ/dc = 9
- In code, the derivative of the final output J with respect to a variable var is just named `dvar` — e.g., `da`, `db`, `dc`.

### 5. 验证直觉 | Sanity Check by Nudging

- 验证 dJ/db = 6:b 从 3 → 3.001,则 u: 6 → 6.002,v: 11 → 11.002,J: 33 → 33.006 ✓(J 的变化是 b 的 6 倍)
- Nudge b from 3 to 3.001: u becomes 6.002, v becomes 11.002, J becomes 33.006 — J moved 6× the nudge, confirming dJ/db = 6.

## 名词解释 | Glossary

| 英文 | 中文 | 解释 |
|---|---|---|
| Chain Rule | 链式法则 | 复合影响的导数 = 各段导数相乘,如 dJ/da = dJ/dv · dv/da |
| Backward Pass / Backpropagation | 反向传播 | 从输出端往回逐层求导的过程 |
| Right-to-left Pass | 从右到左遍历 | 求导的高效顺序:先算离输出近的导数再复用 |
| dJ/dv | 中间导数 | J 对中间变量的导数,可被后续步骤复用 |
| Final Output Variable | 最终输出变量 | 我们关心的量(J);所有导数都是"J 对谁" |
| dvar(代码记号) | 导数变量名 | 代码中 dJ/d(var) 简写为 dvar,如 da、db、dc |
| Reuse of Computation | 计算复用 | 反向传播高效的原因:中间导数只算一次 |
| Nudge Check | 轻推验证 | 把变量+0.001 看 J 变化多少倍,验证导数值 |
