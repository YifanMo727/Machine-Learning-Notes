# Vectorizing Logistic Regression | 向量化逻辑回归

> Andrew Ng《深度学习专项课程》Course 1, Week 2, Lesson 13 (C1W2L13)
> 视频:https://www.youtube.com/watch?v=okpqeEUdEkY
> ⭐ 消灭样本循环:一行代码算完 m 个样本的前向传播

## 核心知识点 | Key Points

### 1. 目标 | The Goal

- 前向传播要对每个样本算 z⁽ⁱ⁾ = wᵀx⁽ⁱ⁾ + b 和 a⁽ⁱ⁾ = σ(z⁽ⁱ⁾),共 m 次。
- 本讲目标:**不用任何 for 循环**,一次算完所有样本。
- Forward propagation needs z(i) and a(i) for every example. Goal: compute all m of them without a single for loop.

### 2. 关键:利用矩阵 X 的列结构 | Use the Column Structure of X ⭐

- 回忆:X 的形状是 (n_x, m),**每列一个样本**。
- 把所有 z 排成一行 Z = [z⁽¹⁾, z⁽²⁾, …, z⁽ᵐ⁾](形状 1×m),则:
  - **Z = wᵀX + [b, b, …, b]**
- 为什么成立:wᵀ(1×n_x)乘 X(n_x×m),按矩阵乘法规则,结果的第 i 个元素正是 wᵀx⁽ⁱ⁾——**矩阵乘法自动对每一列做了内积**。
- Stack the z's into a 1×m row vector Z. Then Z = wᵀX + [b … b]: matrix multiplication automatically takes the inner product of w with every column of X.

### 3. numpy 实现与广播 | The numpy Line & Broadcasting

```python
Z = np.dot(w.T, X) + b
```

- 这里 b 只是**一个实数**,但 numpy 会自动把它**扩展 (expand)** 成 [b, b, …, b] 的 1×m 行向量再相加——这个机制叫**广播 (broadcasting)**,后面有专门一讲。
- In `Z = np.dot(w.T, X) + b`, b is just a real number; Python automatically expands it into a 1×m row vector. This is called broadcasting (covered in detail later).

### 4. 激活值也一次算完 | All Activations at Once

- 把所有 a 也排成一行 A = [a⁽¹⁾, a⁽²⁾, …, a⁽ᵐ⁾](1×m):
  - **A = σ(Z)**——σ 作为逐元素函数,同时作用在 Z 的每个元素上。
- 至此,**m 个样本的全部预测值,两行代码,零循环**:

```python
Z = np.dot(w.T, X) + b
A = sigma(Z)
```

- Stack the activations into A = σ(Z), applying sigmoid elementwise to all of Z. All m predictions in two lines, zero loops.

### 5. 下一步 | What's Next

- 前向传播已向量化;下一讲把**反向传播(梯度计算)**也向量化,彻底告别 for 循环。
- Forward propagation is now vectorized; next lecture vectorizes the gradient computation too.

## 名词解释 | Glossary

| 英文 | 中文 | 解释 |
|---|---|---|
| Z (capital) | 大写 Z | 所有样本的 z 排成的 1×m 行向量 |
| A (capital) | 大写 A | 所有样本的激活值排成的 1×m 行向量 |
| Z = wᵀX + b | 向量化前向公式 | 矩阵乘法自动对 X 每列做内积 |
| np.dot(w.T, X) | numpy 实现 | wᵀX 的代码写法 |
| Broadcasting | 广播 | numpy 自动把实数 b 扩展成向量参与运算的机制 |
| Vectorized Sigmoid | 向量化 sigmoid | σ 逐元素作用于整个 Z,得到 A |
| Forward Propagation (vectorized) | 向量化前向传播 | 两行代码算完全部样本的预测 |
